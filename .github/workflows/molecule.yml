on: push

jobs:
  lint:
    runs-on: geerlingguy/docker-ubuntu2004-ansible:latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
        with:
          path: "${{ github.repository }}"
      - name: molecule
        uses: robertdebock/molecule-action@2.3.4
        with:
          command: lint
  test:
    needs:
      - lint
    runs-on: geerlingguy/docker-ubuntu2004-ansible:latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
        with:
          path: "${{ github.repository }}"
      - name: molecule
        uses: robertdebock/molecule-action@2.3.4
        with:
          command: test
